# RANGE SELECT

```vba
Range("A3").Select
```

After this, `ActiveCell` refers to currently selected cell.

If selected section:

```vba
Range("A3:F3").Select
```

`Selection` refers to active selection.
